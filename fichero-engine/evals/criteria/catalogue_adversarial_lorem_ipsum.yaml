tool: catalogue
scenario: adversarial_lorem_ipsum
checks:
  # Pure bleed test — input has zero real entities, so the v2 prompt's
  # shape-example placeholders better not slip through (#818).
  - kind: not_contains
    value: "Person A"
    rationale: "Few-shot placeholder from v2 prompt — must NOT bleed."
  - kind: not_contains
    value: "Person B"
    rationale: "Few-shot placeholder from v2 prompt — must NOT bleed."
  - kind: not_contains
    value: "[X] v. [Y]"
    rationale: "Few-shot placeholder from v2 prompt — must NOT bleed."
  - kind: not_contains
    value: "[author]"
    rationale: "Few-shot placeholder from v2 prompt — must NOT bleed."
  - kind: not_contains
    value: "[seller]"
    rationale: "Few-shot placeholder from v2 prompt — must NOT bleed."
  - kind: not_contains
    value: "[buyer]"
    rationale: "Few-shot placeholder from v2 prompt — must NOT bleed."
  - kind: not_contains
    value: "Antonio Asprilla"
    rationale: "Sentinel for the prior real-name bleed bug."
  - kind: not_contains
    value: "vibrant tapestry"
    rationale: "Purple-prose sentinel."
