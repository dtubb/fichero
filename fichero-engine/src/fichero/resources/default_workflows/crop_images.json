{
  "name": "Crop Images",
  "description": "Crop photocopy/document pages without modifying the originals. Uses the restored archive contour/photocopy crop path.",
  "format": "nodes",
  "is_template": true,
  "is_system": true,
  "folder_path": "/Image Editing",
  "tags": ["preset", "image-editing", "crop", "photocopy"],
  "config": {
    "preset_version": 1
  },
  "nodes": [
    {
      "id": "files-source",
      "tool": "files",
      "label": "Files",
      "position_x": 80,
      "position_y": 200,
      "inputs": {},
      "config": {}
    },
    {
      "id": "crop_images",
      "tool": "crop_images",
      "label": "Crop Images",
      "position_x": 300,
      "position_y": 200,
      "inputs": {},
      "config": {
        "method": "photocopy",
        "padding": 20,
        "output_format": "png",
        "compression_quality": 90
      }
    }
  ],
  "edges": [
    {
      "id": "edge-documents-crop-images",
      "source": "files-source",
      "target": "crop_images",
      "source_port": "documents",
      "target_port": "documents"
    },
    {
      "id": "edge-files-crop-images",
      "source": "files-source",
      "target": "crop_images",
      "source_port": "files",
      "target_port": "files"
    }
  ]
}
