{
  "name": "Transcribe",
  "description": "Transcribe every file using a cloud vision LLM (e.g. GPT-4o, Claude Sonnet). Better at handwriting and historical scripts than Apple Vision; uses the provider/model set on the workflow.",
  "format": "nodes",
  "is_template": true,
  "is_system": true,
  "folder_path": "/Transcribe",
  "tags": [
    "preset",
    "ocr",
    "cloud"
  ],
  "nodes": [
    {
      "id": "files-source",
      "tool": "files",
      "label": "Files",
      "position_x": 80,
      "position_y": 160,
      "inputs": {},
      "config": {}
    },
    {
      "id": "transcribe",
      "tool": "transcribe",
      "label": "Transcribe (cloud)",
      "position_x": 240,
      "position_y": 160,
      "inputs": {},
      "config": {
        "vision_mode": "llm",
        "language": "en",
        "update_page_content": true
      }
    }
  ],
  "edges": [
    {
      "id": "edge-1",
      "source": "files-source",
      "target": "transcribe",
      "source_port": "files",
      "target_port": "files"
    }
  ],
  "config": {
    "preset_version": 2
  }
}
