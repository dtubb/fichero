# Done

- Implemented the stage-A `#1103` backend slice for first-class references.
- Added `Reference` and `ReferenceProvenance` storage models, DuckDB migrations, and `/api/references` plus `/api/documents/{id}/citations` routes.
- Added focused regression coverage and regenerated the OpenAPI contract + generated client artifacts.
