# Done

- Implemented the audit-log slice for issue #1089.
- Agent write audit records now carry optional `document_id` and `artifact_id` fields and can be filtered by them via `GET /api/agents/write/audit`.
- Added regression coverage for the new audit filter and regenerated the OpenAPI/client artifacts.
