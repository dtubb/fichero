# done-codex-2026-05-25-1353

Fixed the digest review blocker on `codex`.

## #1198 digest guard
- Changed `_digest_library_database` to declare `Header(default=None, alias="X-Fichero-Library-Path")` on its own param so missing-header requests now reach the explicit `400` branch.
- Kept DB resolution direct inside the helper, after the allowlist check, instead of routing through another required-header dependency.
- Added a one-line comment on `library_path` clarifying it is display-label only, not used for auth or DB resolution.

## Verification
- `PYTHONPATH=fichero-engine/src .venv/bin/pytest fichero-engine/tests/unit/test_routes_entities_kg_integration.py -k digest -q`
- `PYTHONPATH=fichero-engine/src .venv/bin/ruff check fichero-engine/src/fichero/api/routes/entities.py`
