# Done: bibliographic canonical BibTeX slice

- Added canonical `bibtex` storage to `SourceMetadata`.
- `render_bibtex()` now prefers stored canonical BibTeX when present.
- `ingest_file()` now loads same-stem bibliography sidecars before extraction.
- Added focused regression tests for metadata, rendering, and sidecar ingest.
