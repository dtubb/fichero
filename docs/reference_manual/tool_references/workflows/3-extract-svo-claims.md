<!-- GENERATED by scripts/generate_capability_reference.py — do not edit by hand. -->

# 3 · Extract SVO → Claims

> 🤖 *AI Drafted (Not reviewed)*

Step 3 of the discrete import-artifacts pipeline. Reuse the transcription artifacts from step 1 plus the persisted entity rows from step 2 to extract and persist SVO KnowledgeClaim rows only. Safe to re-run: the canonical claim writer deduplicates repeated page-scoped claims.

| | |
| --- | --- |
| Folder | /Catalogue |
| Steps | 2 |
| Tags | preset, catalogue, stage, svo, claims, reviewable |

## Steps, in run order

### 1. Files

Tool: [Files](../tools/files.md) — Pass through input files from workflow context

### 2. Extract SVO claims

Tool: [Extract SVO Claims](../tools/extract_svo_only.md) — Extract and persist SVO KnowledgeClaim rows only from existing entities and transcription artifacts

Settings this step uses:

| Option | Value |
| --- | --- |
| `output_language` | auto |
| `provider_name` | $small |
