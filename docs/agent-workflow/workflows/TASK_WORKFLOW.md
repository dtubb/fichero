# TASK_WORKFLOW

1. Pick the highest-priority open item in `docs/agent-workflow/TODO.md`.
2. Mark it in-progress.
3. Implement changes in small, reviewable commits.
4. Validate with project checks (`xcodebuild`, `swiftlint`, `pytest`, `pylint`) as applicable.
5. Update docs/references for any moved files.
6. Mark the task complete and commit.
