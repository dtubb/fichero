# INBOX_WORKFLOW

1. Check `docs/agent-workflow/inbox/` for new notes.
2. If an item is actionable, add/update an entry in `docs/agent-workflow/TODO.md`.
3. Move or copy the note into a task folder outside the repo (archive/delete staging) if needed.
4. Keep only active, lightweight planning notes in `docs/agent-workflow/inbox/`.
5. Commit doc updates.
