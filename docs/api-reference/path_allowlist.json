{
  "_doc": "Public OpenAPI paths not yet mentioned verbatim in docs/api-reference/*.md. Drop an entry once the exact path string is documented there. CI-enforced.",
  "paths": {
    "/api/actions": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/audit": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/audit/{audit_id}/undo": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/builtin": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/categories": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/category/{category}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/composite": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/custom": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/from-node": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/import": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/invoke": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/popular": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/recent": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/registry": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/search": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/{action_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/{action_id}/export": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/actions/{action_id}/use": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/activity": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/activity/batch/{batch_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/activity/cleanup": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/activity/entity-types": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/activity/feed": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/activity/metrics/summary": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/activity/recent": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/activity/stats": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/activity/stream": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/activity/top": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/activity/trends": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/activity/workflow/{workflow_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/agent-memory": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/agent-memory/{note_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/agents/write": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/agents/write/approve": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/agents/write/audit": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/agents/write/audit/{record_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/annotations": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/annotations/crop": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/annotations/{annotation_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/annotations/{annotation_id}/crop": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/annotations/{annotation_id}/promote-to-claim": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/artifacts/": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/artifacts/document/{doc_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/artifacts/types": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/artifacts/{artifact_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/auth/login": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/auth/logout": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/auth/me": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/authz/library": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/authz/members": "Library sharing members read API (#2869); route-family documented, per-path markdown callout pending.",
    "/api/batches": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/batches/{batch_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/batches/{batch_id}/cancel": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/batches/{batch_id}/execute": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/batches/{batch_id}/pause": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/batches/{batch_id}/progress": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/batches/{batch_id}/resume": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/batches/{batch_id}/retry": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/bibliography/document/{document_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/bibliography/document/{document_id}/attach": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/bibliography/document/{document_id}/extract": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/bibliography/export.bib": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/bibliography/import": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/bibliography/resolve": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/chains": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/chains/executions/{execution_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/chains/presets/paleography": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/chains/{chain_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/chains/{chain_id}/execute": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/changes/stream": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/chat": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/chat/conversations": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/chat/conversations/reorder": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/chat/conversations/{conversation_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/chat/conversations/{conversation_id}/duplicate": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/chat/extract-text": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/chat/providers": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/citation-usages": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/citations/document/{document_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/citations/document/{document_id}.bib": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/citations/export": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/citations/graph": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/citations/graph/document/{document_id}/inbound": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/citations/graph/document/{document_id}/outbound": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/citations/graph/{citation_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/claim-links/{link_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/claims": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/claims/assign-time-period": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/claims/assign-time-period-from-metadata": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/claims/batch/transition": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/claims/queues/curated": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/claims/queues/rejected": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/claims/queues/shortlisted": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/claims/queues/unreviewed": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/claims/resolve-source": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/claims/{claim_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/claims/{claim_id}/links": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/claims/{claim_id}/related": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/claims/{claim_id}/transition": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/classifications": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/classifications/{value_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/batch-exclude": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/cleanup-orphans": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/collections": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/import": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/pdfs/backfill-pages": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/reorder": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/roots": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/trash": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/workspaces": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}/ancestors": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}/children": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}/geo": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}/move": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}/notes": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}/page-ranges": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}/page-ranges/at/{page}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}/parent": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}/prototype": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}/purge": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}/related": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}/restore": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}/workflow-runs": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}/workspace": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{doc_id}/workspace/items": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{document_id}/citations": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{document_id}/inspector": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{document_id}/knowledge-graph": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{document_id}/outline": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/documents/{document_id}/rollup": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/entities": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/entities/alias-map": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/entities/claim-counts": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/entities/digest": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/entities/resolve/{value}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/entities/top": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/entities/{entity_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/entities/{entity_id}/aliases": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/entities/{entity_id}/biography": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/entities/{entity_id}/co-occurrence": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/entities/{entity_id}/documents": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/entities/{entity_id}/drill-down": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/entities/{entity_id}/inspector": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/export/eleventy-site": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/export/excel": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/export/markdown-folder": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/export/word": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/folders/{entity_type}/folders": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/folders/{entity_type}/move": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/folders/{folder_id}/views": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/health": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/hermeneutics/circle-state": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/hermeneutics/circle-state/{state_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/hermeneutics/circle-state/{state_id}/backtrack": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/hermeneutics/circle-state/{state_id}/navigate": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/hermeneutics/frameworks": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/hermeneutics/frameworks/{framework_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/hermeneutics/interpretations": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/hermeneutics/interpretations/{interpretation_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/hermeneutics/patterns": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/hermeneutics/patterns/{pattern_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/hermeneutics/patterns/{pattern_id}/claims/{claim_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/hermeneutics/suggestions": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/hermeneutics/taxonomy/methods": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/iiif/iiif/image/{document_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/iiif/iiif/manifest/{document_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/iiif/iiif/{identifier}/info.json": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/iiif/iiif/{identifier}/{region}/{size}/{rotation}/{quality}.{format}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/images/{document_id}/edits": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/images/{document_id}/operations/crop": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/images/{document_id}/operations/enhance": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/images/{document_id}/operations/remove-background": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/images/{document_id}/operations/rotate": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/images/{document_id}/operations/segment": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/images/{document_id}/operations/straighten": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/images/{document_id}/preview": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/ingest/file": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/ingest/folder": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/ingest/status/{task_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/ingest/xlsx": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/integrations": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/integrations/available": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/integrations/bookends/citation/{external_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/integrations/bookends/libraries": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/integrations/devonthink/databases": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/integrations/tinderbox/documents": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/integrations/tinderbox/notes": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/integrations/tinderbox/notes/{external_id}/attributes": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/integrations/{app_name}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/integrations/{app_name}/export": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/integrations/{app_name}/import": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/integrations/{app_name}/items": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/integrations/{app_name}/items/{external_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/integrations/{app_name}/open/{external_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/integrations/{app_name}/refresh": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/claim-analysis/{claim_id}/contradictions": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/claim-analysis/{claim_id}/evidence-chain": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/claim-search": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/claim-search/embed": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/claim-search/{claim_id}/similar": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/claims/batch-curation": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/claims/merge": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/claims/prune-trivial": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/claims/unmerge": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/curation-rules/claim-rules": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/curation-rules/claim-rules/batch": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/curation-rules/entity-rules": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/curation-rules/entity-rules/batch": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/entities/batch-curation": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/entities/{entity_id}/bio": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/entity-curation/audit": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/entity-curation/audit/{audit_id}/undo": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/entity-curation/candidates": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/entity-curation/merge": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/entity-curation/semantic": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/entity-curation/semantic/embed": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/entity-curation/split": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/export/rdf": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/graph/centrality": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/graph/clustering": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/graph/communities": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/graph/components": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/graph/cooccurrence/{entity_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/graph/metrics": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/graph/neighborhood/{entity_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/graph/pagerank": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/graph/path": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/graph/similar/{entity_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/graph/traverse/{entity_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/graph/triangles/{entity_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/inclusion": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/interpretations/circle-state": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/interpretations/circle-state/{state_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/interpretations/circle-state/{state_id}/backtrack": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/interpretations/circle-state/{state_id}/navigate": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/interpretations/frameworks": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/interpretations/frameworks/{framework_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/interpretations/interpretations": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/interpretations/interpretations/{interpretation_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/interpretations/patterns": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/interpretations/patterns/{pattern_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/interpretations/patterns/{pattern_id}/claims/{claim_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/interpretations/suggestions": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/interpretations/taxonomy/methods": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/mutations": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/mutations/{mutation_id}/undo": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/predictions": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/predictions/heuristic": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/predictions/{run_id}/apply": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/pykeen/models/{model_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/pykeen/predict/{entity_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/pykeen/stored": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/pykeen/stored/{prediction_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/pykeen/stored/{prediction_id}/verify": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/pykeen/train": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/pykeen/training-jobs": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/pykeen/training-jobs/{model_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/query/examples": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/query/sparql": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/rebuild": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/render/paragraph": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/reset": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/review/graph-candidates": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/review/labels": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/review/pairs": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/review/pairs/{pair_id}/accept": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/review/pairs/{pair_id}/reject": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/review/summary": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/search": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/sparql": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/triangulation": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/triangulation/entity/{entity_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/kg/triangulation/recompute": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/libraries/{lib}/entity-types": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/libraries/{lib}/entity-types/{entity_type_key}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/library": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/library-items/{item_id}/links": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/library/links": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/library/links/{link_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/local-inference/catalog": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/local-inference/profiles": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/local-inference/profiles/validate": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/local-inference/runtime": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/local-inference/runtime/provision": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/local-inference/profiles/{profile_id}/health": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/local-inference/profiles/{profile_id}/start": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/local-inference/profiles/{profile_id}/status": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/local-inference/profiles/{profile_id}/stop": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/local-models": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/local-models/disk-usage": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/local-models/download/{model_type}/{model_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/local-models/{model_type}/{model_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mcp-servers": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mcp-servers/tools/all": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mcp-servers/tools/load-into-workflow-registry": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mcp-servers/tools/reload-workflow-registry": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mcp-servers/{server_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mcp-servers/{server_id}/load-tools": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mcp/tools/knowledge/claims": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mcp/tools/knowledge/claims/create": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mcp/tools/knowledge/claims/{claim_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mcp/tools/knowledge/entities": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mcp/tools/knowledge/entities/upsert": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mcp/tools/knowledge/entities/{entity_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/migrations/migrations": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/migrations/migrations/integrity-check": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/migrations/migrations/rollback": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/migrations/migrations/run": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/migrations/migrations/status/{run_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/migrations/migrations/validate": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/connections": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/connections/{connection_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/export/tinderbox": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/canvas/folders/{scope_id}/arrange": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/canvas/folders/{scope_id}/items": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/canvas/folders/{scope_id}/items/{item_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/canvas/folders/{scope_id}/layout": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/import/tinderbox": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/nodes": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/nodes/{node_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/notes": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/notes/{note_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/rooms": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/rooms/{room_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/rooms/{room_id}/capture": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/rooms/{room_id}/focus": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/rooms/{room_id}/scene": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/rooms/{room_id}/suggest-arrangement": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/rooms/{room_id}/viewport/{user_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/stacks": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/stacks/{stack_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mind-palace/stacks/{stack_id}/nodes/{node_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/mindpalace/render": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/model-comparison/compare": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/model-comparison/compare-node": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/model-comparison/compare-node/apply": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/model-comparison/compare-tool": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/model-comparison/compare-vision": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/model-comparison/compare-workflow": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/model-comparison/comparison/{comparison_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/model-comparison/estimate-cost": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/model-comparison/history": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/model-comparison/language-fit": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/model-comparison/models": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/model-comparison/models-by-tier": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/model-comparison/presets": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/model-comparison/recommend-models": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/model-comparison/tools": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/models/huggingface": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/models/huggingface/tasks": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/models/huggingface/{model_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/multilingual/claims": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/multilingual/detect": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/multilingual/entities": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/multilingual/entities/search": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/multilingual/normalize": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/multilingual/transliterate": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/notes": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/notes/{note_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/notes/{note_id}/backlinks": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/notes/{note_id}/forward-links": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/notes/{note_id}/links": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/notes/{note_id}/links/{link_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/pair": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/pair/code": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/pair/devices": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/pair/devices/renew": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/pair/devices/{device_id}/revoke": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/policies/orchestration": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/policies/orchestration/evaluate": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/policies/orchestration/{rule_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/projects": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/projects/membership/{target_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/projects/{project_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/projects/{project_id}/include": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/projects/{project_id}/include/{inclusion_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/projects/{project_id}/items": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/providers": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/providers/apple-intelligence/probe": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/providers/catalog": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/providers/catalog/{provider_type}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/providers/models/{provider_type}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/providers/refs": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/providers/refs/{ref_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/providers/{provider_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/providers/{provider_id}/models": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/providers/{provider_id}/models/{model_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/providers/{provider_type}/api-key": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/providers/{provider_type}/api-key/status": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/providers/{provider_type}/test": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/references": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/references/{reference_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/registries/claim-kinds": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/registries/claim-kinds/{value_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/registries/epistemic-statuses": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/registries/epistemic-statuses/{value_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/registry": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/registry/add": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/registry/unicode-collisions": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/registry/unicode-collisions/merge": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/registry/update-access": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/registry/{library_path}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/checklists": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/checklists/{checklist_id}/items/{item_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/notes": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/notes/{note_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/plans": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/plans/{plan_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/plans/{plan_id}/tasks": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/projects": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/projects/{project_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/projects/{project_id}/checklists": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/projects/{project_id}/notes": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/projects/{project_id}/plans": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/projects/{project_id}/sources": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/projects/{project_id}/tasks": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/sources": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/steps": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/steps/{step_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/tasks": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/tasks/{task_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/tasks/{task_id}/steps": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/tools/browser-navigate": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/tools/browser-save": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/tools/document-fetch": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/research/tools/web-search": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/schedules": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/schedules/{schedule_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/schedules/{schedule_id}/pause": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/schedules/{schedule_id}/resume": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/schedules/{schedule_id}/runs": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/schedules/{schedule_id}/trigger": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/search": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/search/embed/{doc_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/search/explain": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/search/explain/{query}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/search/keywords": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/search/metrics": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/search/modes": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/search/reindex": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/search/stats": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/search/views": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/search/views/grid": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/search/views/map": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/search/views/table": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/settings/ai-defaults": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/settings/ai-defaults/repair": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/settings/model-profiles": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/settings/model-profiles/{profile_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/sources": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/sources/{source_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/stats": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/storage/debug/{doc_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/storage/display/{doc_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/storage/regenerate-missing": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/storage/snapshots": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/storage/snapshots/{snapshot_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/storage/snapshots/{snapshot_id}/pin": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/storage/snapshots/{snapshot_id}/restore": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/storage/source/{doc_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/storage/stats": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/storage/thumbnail/{doc_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/tasks/tasks": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/tasks/tasks/health": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/tasks/tasks/kg-metrics": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/tasks/tasks/kg-metrics/{task_id}/data": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/tasks/tasks/metrics": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/tasks/tasks/metrics/{task_id}/data": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/tasks/tasks/reindex": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/tasks/tasks/reindex/{task_id}/progress": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/tasks/tasks/vector-repair": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/tasks/tasks/vector-repair/{task_id}/progress": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/tasks/tasks/{task_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/tasks/tasks/{task_id}/cancel": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/tasks/tasks/{task_id}/result": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/triggers": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/triggers/{trigger_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/triggers/{trigger_id}/executions": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/triggers/{trigger_id}/pause": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/triggers/{trigger_id}/resume": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/users": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/users/{user_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/cache": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/cache/stats": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/execute": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/stream/{thread_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/threads": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/threads/{thread_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/threads/{thread_id}/cancel": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/threads/{thread_id}/diagram.png": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/threads/{thread_id}/diagram.svg": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/threads/{thread_id}/history": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/threads/{thread_id}/pause": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/threads/{thread_id}/resume": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/threads/{thread_id}/run": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/threads/{thread_id}/status": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/workflows/{workflow_id}/cache": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/workflows/{workflow_id}/cache/stats": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/workflows/{workflow_id}/code": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/workflows/{workflow_id}/visualization": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflow-execution/workflows/{workflow_id}/visualization.png": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflows": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflows/import": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflows/modes": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflows/reinstall-defaults": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflows/reorder": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflows/tools": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflows/tools/grouped": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflows/tools/{tool_name}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflows/tools/{tool_name}/create-node": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflows/tools/{tool_name}/prompt": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflows/{workflow_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflows/{workflow_id}/duplicate": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflows/{workflow_id}/estimate-cost": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/workflows/{workflow_id}/export": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/view/document/{doc_id}": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/view/kg/global": "Current API reference index documents route families and the committed schema render, not per-path markdown callouts yet.",
    "/api/authz/share": "CLI/auth endpoint; route-family documented, per-path callout pending."
  }
}
