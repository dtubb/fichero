# OS / editor
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.vscode/
.cursor/
.idea/
*.swp
*.swo

# Python
__pycache__/
*.py[cod]
*.egg-info/
*.dist-info/
.pytest_cache/
.ruff_cache/
.venv/
.venv-deps/
fichero-server/.venv/
venv/
.ipynb_checkpoints/
fichero-server/.briefcase/
fichero-server/.briefcase-venv/
fichero-server/.pytest_cache/
fichero-server/.mypy_cache/
# Disposable base path for the Swift integration-test harness (EngineHarness)
fichero-server/.itest-base/

# Build / generated
.build/
build/
dist/
logs/
fichero-server/src/fichero_server/cli/generated/

# MkDocs site (build output + local docs venv)
_site_build/
.venv-docs/

# Swift / Xcode user-local
**/xcuserdata/
**/*.xcuserstate
**/.swiftpm/xcode/xcuserdata/
fichero-swiftui/derived_data/
fichero-swiftui/fichero-swiftui/Resources/FicheroBackend.app/
# Local tooling

.aider*
opencode.json

# Project archives / large references
sample_code/

# Real archival corpus — kept out of the open-source tree (engineering notes stay)
agent-work/icanh-clean/source/

# Legacy paths still seen in historical docs
old_tools/
old code/
old documents/
examples/
toga-examples/

# Graphify knowledge graph (machine-generated, ~17MB)
graphify-out/

# Private agent / harness config — kept on disk, out of the public tree.
# The agent *workflow + skills* ship under `agents/`; the per-machine harness
# config and running state do not. (.ai/ is loop/inbox scratch — fully ignored.)
.claude/
.codex/
.ai/
.session-end-complete

# Misc
output.txt
cpu_worker.log
io_worker.log
dump.rdb
.empty/
.mcp.json
.claude/settings.local.json
.claude/settings.*.bak

default.profraw
.claude/worktrees/
.session-end-complete
.claude/scheduled_tasks.lock
CONTINUE.md
agent-work/.cascade.lock

# Autoloop run logs — regenerable, not version-controlled
.claude/loop-logs/

# Session-local block signal (never commit — see BLOCK.md semantics)
BLOCK.md
.kreuzberg/
.swiftlint-cache/

# release-notes generator cursor (local state)
.release-notes-cursor
profile/

# internal agent orchestration (not for public repo)
PLAN-GOVERNANCE.md
STATE.md
MEMORY.md

# engine runtime/build artifacts
fichero-server/logs/
fichero-server/dist/
fichero-server/build/

# internal agent scratch (local only)
agent-work/
HISTORY.md
HISTORY*.md
.build-ios/

# agent worker briefs (internal, per-worktree)
.inspector-worker-brief.md
.worker-brief.md
