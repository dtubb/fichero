# src/link/sb_link.py

# Import SlipBox’s common functions and constants.
from src.common import *

def sb_link(inputs, output, *, length=5):
  """Extract key points"""
  
  # Print statement to confirm extraction
  verbose_print(f'Linking {length} points from {inputs} into {output}', VERBOSITY_LEVELS.INFO)
  
  pass