2026-06-28: Added focused room-node bridge regression tests for F5 slice-1. Covered folded `SpatialRoom` read/update preservation of backing `Document.parent_id`, invalid folded room payload raise paths, reopen backfill of legacy `SpatialRoom` rows into `room` prototype documents, and `/api/mind-palace/rooms` list/get/update parity against document-backed rooms. Docs work is drained; current lane is backend regression tests.
