2026-07-03: Routed `DELETE /api/entities/{id}` through `registry.invoke` as audited `entity.delete`, flipped the existing strict xfail, and extended the mutation audit lock to cover entity delete; targeted proof: `test_node_model_action_audit.py` + `test_mutation_audit_lock.py` -> 40 passed.
2026-07-03: Moved note/document/entity create-update-delete and document.move emits onto `ChangeSpec.emit_type` so emit happens only after audit in `registry.invoke`; refreshed `test_mutation_atomicity.py` to lock the new best-effort emit contract. Targeted proof: audit lock + node-model audit + atomicity -> 48 passed, 4 xfailed (remaining xfails are the real audit-write rollback gap).
