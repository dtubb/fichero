2026-06-28: Added route/handler-level fold coverage for saved searches and research workspaces. `test_routes_search.py` now covers saved-search create/list/folded retrieve round-trip plus the malformed folded query prefer-raise path. `test_routes_research_agents.py` now covers research-workspace create/list/get round-trip, workspace contained items via `/api/documents/{doc_id}/workspace/items`, and the folded-plan missing-parent raise path. Verified with focused pytest runs on both touched files (green).
