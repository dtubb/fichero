2026-06-28: Added P3/P4 fold coverage in `test_routes_documents.py` and `test_db.py`. Route-level assertions now cover folded note/milestone/entity visibility through the generic document tree, retrieval of folded note and milestone document nodes, and route-visible entity re-filing via updated children lists. DB integration coverage now verifies folded note retrieval for page-scoped notes plus filed-entity retrieval and re-file round-trips, alongside the existing prefer-raise parent-validation paths. Verified with focused pytest runs on both touched files (green).
