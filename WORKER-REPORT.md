2026-06-28: Updated `docs/contributor/node-model.md` for shipped prototype foundation and room bridge work: documented builtin folder/room/research_workspace prototype seeding and inheritance, corrected research plan/task/step fold status to shipped, and documented the node-backed mind-palace room bridge while noting the `/api/mind-palace/rooms*` routes still operate. Re-ran `fichero-engine/tests/contracts/test_docs_coverage.py` with the requested `PYTHONPATH`; it passed, so no API-reference or allowlist changes were needed. `~/.venv/bin/mkdocs build --strict` also passed.
