HANDOFF — 2026-03-13 18:05 ADT
Prepared by: Codex `/session-start-auto`
For: Next hourly automation run

## Where We Are
Executed one additional milestone bugfix for `#311` on branch `feature/issue-310`, on top of earlier `#310` fix work.

## What Changed This Run
- File changed: `fichero-swiftui/fichero-swiftui/Services/EmbeddedBackendService.swift`
- Behavior change:
  - In `DEBUG`, after connecting to an external backend, app now probes `GET /api/workflows`.
  - If probe returns `404`, app logs a warning and falls back to launching embedded backend.
  - Embedded backend launch now defaults `FICHERO_FEATURE_TIER=dev` in `DEBUG` when unset.
- Commit created:
  - `15ba76c2` — `fix: avoid workflow API 404 in debug backend startup (#311)`

## Validation Status
- Passed:
  - `swiftlint lint --no-cache fichero-swiftui/fichero-swiftui/Services/EmbeddedBackendService.swift`
- Blocked in sandbox:
  - `PYTHONPATH=fichero-api/src .venv/bin/ruff check fichero-api/src/` (`.venv/bin/ruff` missing)
  - `PYTHONPATH=fichero-api/src .venv/bin/pytest ...` (`.venv/bin/pytest` missing)
  - `xcodebuild ...` blocked by sandbox-denied cache/log filesystem paths

## To Do First (Next Run)
1. Run full validation in unrestricted local environment:
   - `PYTHONPATH=fichero-api/src .venv/bin/ruff check fichero-api/src/`
   - `PYTHONPATH=fichero-api/src .venv/bin/pytest fichero-api/tests/unit/test_storage.py`
   - `xcodebuild -project fichero-swiftui/fichero-swiftui.xcodeproj -scheme Fichero -configuration Debug -sdk macosx build`
2. Manually verify `#311` behavior:
   - external backend lacking workflow routes should trigger fallback
   - workflow save/list should work after fallback
3. Open/update PR from `feature/issue-310` and post validation evidence to `#310` and `#311`.

## Branch Status
- Current branch: `feature/issue-310`
- Latest commits:
  - `15ba76c2` (`#311` debug startup workflow-route fallback)
  - `40ee2a1c` (`#310` Unicode filename Content-Disposition fix)
