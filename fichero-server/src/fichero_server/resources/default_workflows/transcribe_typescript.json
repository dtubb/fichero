{
  "name": "Transcribe Typescript",
  "description": "Use this when: you have typewritten or printed documents — typescripts, books, pamphlets, official records with clear print. Uses Apple Vision (on-device, fast) when configured; falls back to the LLM vision model for scanned pages with poor contrast.",
  "format": "nodes",
  "is_template": true,
  "is_system": true,
  "folder_path": "/Transcribe",
  "tags": ["preset", "transcribe", "typescript", "ocr", "printed"],
  "config": {
    "preset_version": 4
  },
  "nodes": [
    {
      "id": "files-source",
      "tool": "files",
      "label": "Files",
      "position_x": 80,
      "position_y": 200,
      "inputs": {},
      "config": {}
    },
    {
      "id": "transcribe",
      "tool": "transcribe",
      "label": "Transcribe (typescript)",
      "position_x": 280,
      "position_y": 200,
      "inputs": {},
      "config": {
        "vision_mode": "auto",
        "language": "auto",
        "update_page_content": true,
        "prompt": "Transcribe the text visible on this image. The document is typewritten or printed.\n\nRules:\n- Output ONLY the transcription. No headings, preamble, commentary, or summary.\n- Preserve original layout, line breaks, and paragraph structure.\n- Preserve original spelling, capitalisation, and punctuation exactly as printed.\n- Include every visible text element — headers, body, footers, page numbers, marginalia, stamps, annotations.\n- For text you cannot confidently read, write [ILLEGIBLE] inline at that position.\n- If the image contains no text, output [no text]."
      }
    }
  ],
  "edges": [
    {
      "id": "edge-files-transcribe",
      "source": "files-source",
      "target": "transcribe",
      "source_port": "files",
      "target_port": "files"
    },
    {
      "id": "edge-files-transcribe-documents",
      "source": "files-source",
      "target": "transcribe",
      "source_port": "documents",
      "target_port": "documents"
    }
  ]
}
