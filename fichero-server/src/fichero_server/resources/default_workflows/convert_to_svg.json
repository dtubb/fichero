{
  "name": "AI Redraw as SVG",
  "description": "An AI vision model REDRAWS each image/page as SVG — a generative sketch of the document's layout and text, not a vector trace of the original image; positions and shapes are approximate. Feeds the SVG (WebKit) representation in the document viewer. The model alias is editable on the Convert node.",
  "format": "nodes",
  "is_template": true,
  "is_system": true,
  "folder_path": "/Convert",
  "tags": ["preset", "convert", "svg", "representation"],
  "config": {"preset_version": 2},
  "nodes": [
    {"id": "files-source", "tool": "files", "label": "Files", "position_x": 80, "position_y": 200, "inputs": {}, "config": {}},
    {"id": "convert", "tool": "convert", "label": "AI Redraw as SVG", "position_x": 320, "position_y": 200, "inputs": {}, "config": {"target_format": "svg", "preserve_layout": true}}
  ],
  "edges": [
    {"id": "edge-files-convert", "source": "files-source", "target": "convert", "source_port": "files", "target_port": "files"},
    {"id": "edge-files-convert-documents", "source": "files-source", "target": "convert", "source_port": "documents", "target_port": "documents"}
  ]
}
