{
  "name": "AI Convert to Markdown",
  "description": "An AI vision model reads each image/page and WRITES it out as clean Markdown — a generative re-creation of the content (headings, lists, tables), not a mechanical file conversion; wording and layout are the model's reading of the page. Feeds the Markdown representation in the document viewer. The model alias and layout options are editable on the Convert node.",
  "format": "nodes",
  "is_template": true,
  "is_system": true,
  "folder_path": "/Convert",
  "tags": ["preset", "convert", "markdown", "representation"],
  "config": {"preset_version": 2},
  "nodes": [
    {"id": "files-source", "tool": "files", "label": "Files", "position_x": 80, "position_y": 200, "inputs": {}, "config": {}},
    {"id": "convert", "tool": "convert", "label": "AI Convert to Markdown", "position_x": 320, "position_y": 200, "inputs": {}, "config": {"target_format": "markdown", "preserve_layout": true}}
  ],
  "edges": [
    {"id": "edge-files-convert", "source": "files-source", "target": "convert", "source_port": "files", "target_port": "files"},
    {"id": "edge-files-convert-documents", "source": "files-source", "target": "convert", "source_port": "documents", "target_port": "documents"}
  ]
}
