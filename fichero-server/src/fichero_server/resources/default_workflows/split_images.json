{
  "name": "Split Images",
  "description": "Split images into grid tiles or PDFs into per-page image derivatives without modifying originals. Useful before OCR, segmentation, or recombination workflows.",
  "format": "nodes",
  "is_template": true,
  "is_system": true,
  "folder_path": "/Image Editing",
  "tags": [
    "preset",
    "image-editing",
    "split",
    "pdf",
    "ocr"
  ],
  "config": {
    "preset_version": 1,
    "tested": true
  },
  "nodes": [
    {
      "id": "files-source",
      "tool": "files",
      "label": "Files",
      "position_x": 80,
      "position_y": 200,
      "inputs": {},
      "config": {}
    },
    {
      "id": "split_images",
      "tool": "split_images",
      "label": "Split Images",
      "position_x": 300,
      "position_y": 200,
      "inputs": {},
      "config": {
        "rows": 1,
        "columns": 2,
        "pdf_dpi": 200,
        "output_format": "png",
        "compression_quality": 90
      }
    }
  ],
  "edges": [
    {
      "id": "edge-files-split-images",
      "source": "files-source",
      "target": "split_images",
      "source_port": "files",
      "target_port": "files"
    }
  ]
}
