{
 "name": "Detect Regions (Apple Vision)",
 "description": "Use this when: you want text bounding boxes on pages before (or without) transcription. On-device Apple Vision detects line and word regions and saves them per page — fast, local, free. For hard hands Apple can miss regions; try Detect Regions (VLM) with a vision model of your choice.",
 "format": "nodes",
 "is_template": true,
 "is_system": true,
 "folder_path": "/Detect Regions",
 "tags": [
  "preset",
  "regions",
  "bboxes",
  "ocr",
  "layout"
 ],
 "config": {
  "preset_version": 2
 },
 "nodes": [
  {
   "id": "files-source",
   "tool": "files",
   "label": "Files",
   "position_x": 80,
   "position_y": 200,
   "inputs": {},
   "config": {}
  },
  {
   "id": "detect-regions",
   "tool": "detect_regions",
   "label": "Detect Regions",
   "position_x": 280,
   "position_y": 200,
   "inputs": {},
   "config": {
    "language": "en"
   }
  }
 ],
 "edges": [
  {
   "id": "edge-files-detect-regions",
   "source": "files-source",
   "target": "detect-regions",
   "source_port": "files",
   "target_port": "files"
  },
  {
   "id": "edge-files-detect-regions-documents",
   "source": "files-source",
   "target": "detect-regions",
   "source_port": "documents",
   "target_port": "documents"
  }
 ]
}