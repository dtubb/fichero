{
 "name": "Diary Entries",
 "description": "Transcribe each diary page (reusing any existing text layer), then split every page into per-day entry nodes: one child per dated entry, carrying a date attribute (diary_entry document type, date role) and the day's bounding box on the page. The entries render immediately in the library's Data view — timeline and calendar. Pages with several dates become several entries; a page that IS one day's entry becomes one. Unreadable dates are kept as undated entries named by the date text on the page, never guessed. The splitter uses the $small model alias — set your default small model in Settings → AI Defaults.",
 "format": "nodes",
 "is_template": true,
 "is_system": true,
 "folder_path": "/Extract",
 "tags": [
  "preset",
  "diary",
  "dates",
  "timeline",
  "small"
 ],
 "config": {
  "preset_version": 1
 },
 "nodes": [
  {
   "id": "files-source",
   "tool": "files",
   "label": "Files",
   "position_x": 80,
   "position_y": 200,
   "inputs": {},
   "config": {}
  },
  {
   "id": "transcribe",
   "tool": "transcribe",
   "label": "Transcribe each page",
   "position_x": 280,
   "position_y": 200,
   "inputs": {},
   "config": {
    "vision_mode": "auto",
    "language": "auto",
    "update_page_content": true
   }
  },
  {
   "id": "diary_entries",
   "tool": "diary_entries",
   "label": "Split Diary Entries",
   "position_x": 480,
   "position_y": 200,
   "inputs": {},
   "config": {
    "provider_name": "$small",
    "prototype_key": "diary_entry"
   }
  }
 ],
 "edges": [
  {
   "id": "edge-files-transcribe-files",
   "source": "files-source",
   "target": "transcribe",
   "source_port": "files",
   "target_port": "files"
  },
  {
   "id": "edge-files-transcribe-documents",
   "source": "files-source",
   "target": "transcribe",
   "source_port": "documents",
   "target_port": "documents"
  },
  {
   "id": "edge-files-diary-documents",
   "source": "files-source",
   "target": "diary_entries",
   "source_port": "documents",
   "target_port": "documents"
  },
  {
   "id": "edge-transcribe-diary-text",
   "source": "transcribe",
   "target": "diary_entries",
   "source_port": "text",
   "target_port": "text"
  }
 ]
}
