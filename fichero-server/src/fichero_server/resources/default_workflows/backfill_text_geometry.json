{
 "name": "Backfill Text Geometry",
 "description": "Use this when: pages already have a transcription but no word boxes. Apple Vision detects regions on the page (free, on-device), then the existing text is aligned onto those boxes, so every word in the transcript gets a position — measured where Vision read the same word, interpolated between anchors where it did not. Boxes are saved as provider aligned:apple_vision, never as a measured OCR pass, and a page whose alignment cannot be trusted is refused rather than given a wrong overlay.",
 "format": "nodes",
 "is_template": true,
 "is_system": true,
 "folder_path": "/Detect Regions",
 "tags": [
  "preset",
  "regions",
  "bboxes",
  "backfill",
  "alignment"
 ],
 "config": {
  "preset_version": 1
 },
 "nodes": [
  {
   "id": "files-source",
   "tool": "files",
   "label": "Files",
   "position_x": 80,
   "position_y": 200,
   "inputs": {},
   "config": {}
  },
  {
   "id": "detect-regions",
   "tool": "detect_regions",
   "label": "Detect Regions",
   "position_x": 280,
   "position_y": 200,
   "inputs": {},
   "config": {
    "language": "en"
   }
  },
  {
   "id": "merge-geometry",
   "tool": "merge_geometry",
   "label": "Merge Geometry",
   "position_x": 500,
   "position_y": 200,
   "inputs": {},
   "config": {
    "text_artifact_type": "transcription",
    "geometry_artifact_type": "regions"
   }
  }
 ],
 "edges": [
  {
   "id": "edge-files-detect-regions",
   "source": "files-source",
   "target": "detect-regions",
   "source_port": "files",
   "target_port": "files"
  },
  {
   "id": "edge-files-detect-regions-documents",
   "source": "files-source",
   "target": "detect-regions",
   "source_port": "documents",
   "target_port": "documents"
  },
  {
   "id": "edge-detect-regions-merge-geometry",
   "source": "detect-regions",
   "target": "merge-geometry",
   "source_port": "documents",
   "target_port": "documents"
  }
 ]
}
