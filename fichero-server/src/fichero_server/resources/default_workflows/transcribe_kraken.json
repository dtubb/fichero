{
 "name": "Transcribe (Kraken)",
 "description": "Use this when: you want fully on-device HTR from Kraken — its own neural baseline segmentation plus a CC-BY recognition model (McCATMuS), no paid API calls. It reads each line and SAVES the transcript tied to its baseline (per-line geometry the reader can overlay). Install the Kraken runtime AND a recognition model first from Settings → AI → Local Inference (the on-device model catalog); the run reports clearly if either is missing. Best on the historical hands McCATMuS was trained on. Pair with a Cleanup pass for hard pages.",
 "format": "nodes",
 "is_template": true,
 "is_system": true,
 "folder_path": "/Transcribe",
 "tags": [
  "preset",
  "transcribe",
  "htr",
  "kraken",
  "historical",
  "local",
  "single-pass"
 ],
 "config": {
  "preset_version": 2
 },
 "nodes": [
  {
   "id": "files-source",
   "tool": "files",
   "label": "Files",
   "position_x": 80,
   "position_y": 200,
   "inputs": {},
   "config": {}
  },
  {
   "id": "kraken-htr",
   "tool": "kraken_htr",
   "label": "Kraken HTR — baseline + recognition",
   "position_x": 320,
   "position_y": 200,
   "inputs": {},
   "config": {
    "kraken_model": "kraken-mccatmus",
    "update_page_content": true
   }
  }
 ],
 "edges": [
  {
   "id": "edge-files-kraken-htr",
   "source": "files-source",
   "target": "kraken-htr",
   "source_port": "files",
   "target_port": "files"
  },
  {
   "id": "edge-files-kraken-htr-documents",
   "source": "files-source",
   "target": "kraken-htr",
   "source_port": "documents",
   "target_port": "documents"
  }
 ]
}
