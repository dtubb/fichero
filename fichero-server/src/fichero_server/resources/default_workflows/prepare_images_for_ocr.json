{
  "name": "Prepare Images for OCR",
  "description": "Normalize image and PDF pages into OCR-ready derived image files without modifying the originals. Applies EXIF rotation, renders PDFs, boosts contrast, and writes prepared JPGs for downstream transcription workflows.",
  "format": "nodes",
  "is_template": true,
  "is_system": true,
  "folder_path": "/Transcribe",
  "tags": ["preset", "transcribe", "ocr", "images", "preparation"],
  "config": {
    "preset_version": 1
  },
  "nodes": [
    {
      "id": "files-source",
      "tool": "files",
      "label": "Files",
      "position_x": 80,
      "position_y": 200,
      "inputs": {},
      "config": {}
    },
    {
      "id": "prepare_images",
      "tool": "prepare_images",
      "label": "Prepare Images for OCR",
      "position_x": 300,
      "position_y": 200,
      "inputs": {},
      "config": {
        "output_format": "jpg",
        "compression_quality": 85,
        "grayscale": true,
        "autocontrast": true,
        "pdf_dpi": 300
      }
    }
  ],
  "edges": [
    {
      "id": "edge-documents-prepare-images",
      "source": "files-source",
      "target": "prepare_images",
      "source_port": "documents",
      "target_port": "documents"
    },
    {
      "id": "edge-files-prepare-images",
      "source": "files-source",
      "target": "prepare_images",
      "source_port": "files",
      "target_port": "files"
    }
  ]
}
