think about how Fichero can be a MCP tool, but also potentially a conduit for MCP for other apps on the mac. e.g bookends, devonthink, tinderbox, mellel, word. But, also other tools that might be avialb;e online. is htere a good tool repostiry I coudl add? like proviuders. but tools? also how might we leverage the new Agentic model. e.g. goose https://block.github.io/goose/prompt-library. I'm thinking on the one hand, and agent being able to create workflows in the front end os the user interface updates and the model is being designed. also we might think about https://agents.md as well as mistral/devstral and vibe. so, how could fichero be both a mcp server, a mcp tool, a way for users to build langgraphs with mcp tools, but also to all agents to work iwth the fichero data, but also allow the fichero to build agents to work on it. got it. I imagein agents working on tdocuns. but also ficherp being a place toi save agents, store them, build the, deploy them, have them work on a schedule. a bit like hazel, but for agents. got it? a nice visual mac app to do this.