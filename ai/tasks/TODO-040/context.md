# Context for TODO-040: Improve Folder Creation UI to be Inline (Mac-style)

## Background
This task enhances the folder creation UI implemented in TODO-034. Current implementation uses a dialog, but macOS conventions suggest inline editing would provide better user experience.

## What you need to know
- Current state: Folder creation works but uses dialog UI
- Goal: Implement inline editing like macOS Finder
- Pattern: Use "untitled folder" as default name with immediate inline renaming
- Scope: Replace dialog-based creation with inline approach

## Ask if unclear
- Request human input if needed