<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle">
    <channel>
        <title>Fichero Updates</title>
        <link>https://github.com/dtubb/fichero/releases</link>
        <description>Appcast feed for Fichero Sparkle updates.</description>
        <language>en</language>
        <item>
            <title>Fichero 2026.08.22</title>
            <pubDate>Sat, 22 Aug 2026 21:12:11 -0300</pubDate>
            <sparkle:version>2026082201</sparkle:version>
            <sparkle:shortVersionString>2026.08.22</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<p>The bounding-box build. A page's alternative pixels — archival original,
contrast-enhanced, background-removed — are now first-class renditions:
imported from staging sidecars, produced by the image workflows
themselves, listed and served by the engine, and flipped in the preview
with a vertical swipe (↑/↓ keys and chevrons too). Splits made in the
app, by a workflow, or in staging all record one identical geometry, so
"undo the split" is one mechanism and a box drawn on a part means the
same thing everywhere.</p>
<p><strong>Pending human acceptance:</strong> the Marshall visual check — flip a spread
between original / enhanced / split with word boxes showing, and the
boxes stay on the words — has NOT yet been run. Everything below is
verified in isolation; that flip is the acceptance test for this
program and needs a live session.</p>
<ul>
<li>Renditions: engine list + content routes; preview flip via vertical
  swipe/↑↓/chevrons with a slide transition; indicator names the
  rendition and marks cropped/deskewed frames.</li>
<li>Image workflows persist what they make: enhance, fuzzy-clean,
  prepare, rotate, remove-background write renditions (never primary,
  provenance-stamped); split/segment create child nodes with real
  regions; a part that fails to store reaches the caller — no more
  green ticks on runs that changed nothing.</li>
<li>One geometry: in-app crop/split/segment write region_in_parent with
  measured confidence, converged with staged sidecars.</li>
<li>Preview: boxes exact at first fit and at zoom-out; boxes survive page
  steps; tall pages fit both axes; page steps land with the image
  ready; entry highlight is a soft wash behind the words; page arrows
  on images match PDFs; fitted images no longer bounce under
  two-finger swipes; pinch snaps to fit.</li>
<li>Canvas: ⌘-scroll and pinch zoom into the cursor; double-click zooms
  a card to full view and back (context menu too); ⇧⌥ rubber-band in
  3D; drag stays in the board plane with z behind ⌥; render cap
  10,000; halved edge margins; empirical scroll-sign fixes.</li>
<li>Access: libraries survive relaunch (pre-auth grant race fixed);
  link-mode imports ask to persist folder access; a failed original
  prompts for its folder right in the preview; File ▸ Grant Folder
  Access… for manual grants.</li>
<li>Artifacts: drag to Finder/apps exports text or .txt; drop on a
  folder/library promotes to a provenance-stamped node; inspector rows
  select on the name click.</li>
<li>Workflows: force-image-processing genuinely re-runs past the
  skip-if-done cache; transcription geometry survives page steps.</li>
</ul>
]]></description>
            <enclosure
                url="https://github.com/dtubb/fichero/releases/download/v2026.08.22/Fichero.dmg"
                length="567858544"
                type="application/octet-stream"
                sparkle:edSignature="eVRkEWjsqqnDODAg07EgQft49192NBRrdEosaEwWeO3tnyscroqI2nRQUbimUoeyFHDElMNRcKjBK+/xF8NHCg=="
            />
        </item>
        <item>
            <title>Fichero 2026.08.20</title>
            <pubDate>Thu, 20 Aug 2026 15:12:06 -0300</pubDate>
            <sparkle:version>2026082001</sparkle:version>
            <sparkle:shortVersionString>2026.08.20</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<h3>Dev build</h3>
<p>The speed build: a night of profiling the live Marshall corpus (4,169
documents) and removing every whole-collection cost the logs surfaced,
plus the day's live-testing fixes.</p>
<p><strong>Browsing stays fast while the engine works.</strong> Folder listings read the
last-committed snapshot instead of queueing behind an import's embedding
transactions (worst case measured: 12 seconds); each row hydrates once,
without its full page text, through a memoized field plan; vector writes
no longer block database reads; and a new index backs every parent-folder
lookup. Listing routes run off the event loop, so concurrent requests
stop serializing.</p>
<p><strong>Thumbnails land as they're made.</strong> The derivative queue renders a
folder's thumbnails before starting its slower text embeddings, announces
each image the moment it exists, and reports its progress in the toolbar
island ("Processing imported pages — 42%") instead of claiming Ready.
Quitting mid-import is safe: interrupted work resumes on the next launch.
The thumbnail endpoint serves interactive requests instantly and sheds
import-storm misses to the queue (previously: 15,565 calls averaging
784ms, worst 60 seconds).</p>
<p><strong>One request where there were a thousand.</strong> The change-stream patch
flush, workflow-completion refresh, and search resolution now fetch
documents in one batched call each; a live session had issued 1,001
sequential single-document requests.</p>
<p><strong>Search results are nodes.</strong> Every hit — document, person, place, claim,
artifact — resolves into the library grid as a clickable node that
preview, reader, inspector, workflows, and Select All all agree on. The
reader follows result clicks and scrolls to the page; clicking a page in
the reader selects its result row.</p>
<p><strong>Canvas input works like it should.</strong> Two-finger scroll pans both
canvases (the old overlay never actually received scroll events), arrow
keys nudge the camera, ⌘A selects every node, and page thumbnails load
from the library you're in — a wrong-library lookup had silently failed
all 1,500 texture loads in a session. The render cap rises to 1,500.</p>
<p><strong>The reader scrolls like Safari.</strong> Off-screen pages skip layout and
paint entirely, page positions are measured once instead of per scroll
frame, and hidden panels (graph, timeline, map) render only when opened —
crossing a page boundary used to re-run a force-directed graph layout for
a panel that wasn't visible.</p>
<p><strong>Corrections from live testing.</strong> Default workflows appear in every
library again (a legacy flag heal); grouped items stack in place instead
of vanishing to the library root; deleting a corpus can no longer be
raced by its own background embeds resurrecting pages; a stale bookmark
that resolves into the Trash is dropped rather than followed (a library
reference had silently moved to iCloud's Trash); the preview background
follows dark mode; dataset views remember sort and text depth; dataset
cards and sheet rows gain Run Workflow and exclusion menus.</p>
]]></description>
            <enclosure
                url="https://github.com/dtubb/fichero/releases/download/v2026.08.20/Fichero.dmg"
                length="567351460"
                type="application/octet-stream"
                sparkle:edSignature="hHQPDY0WF1IY+u/FgiOcsz2zjmkAQb/9FdXaw0TTW84FntD5yMkEmKywyiC0tSrP7Hp4rdTU2rlIBkIbhwc9BQ=="
            />
        </item>
        <item>
            <title>Fichero 2026.08.17</title>
            <pubDate>Tue, 18 Aug 2026 09:24:56 -0300</pubDate>
            <sparkle:version>2026081701</sparkle:version>
            <sparkle:shortVersionString>2026.08.17</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<h3>Dev build</h3>
<p>The hardening build: two days of live testing on the datasets release,
folded back in — plus the Marshall corpus drop.</p>
<p><strong>Drop a corpus, get a corpus.</strong> Dragging a staged folder with a
<code>manifest.jsonl</code> into a library imports everything it describes — pages,
transcripts as each page's text, deduped entities, renditions — with real
progress counts, thumbnails, and the corpus landing under the folder you
dropped it on. Re-dropping an already-imported corpus repairs it instead
of silently doing nothing. All 29 Marshall diaries are staged and ready.</p>
<p><strong>Sidecars carry their own facts.</strong> A <code>x.jpg.transcript.txt</code> beside an
image becomes its text on any plain folder drop; an <code>.iffy.json</code> stating
<code>original_date</code> dates the document on arrival — no Extract Dates run
needed for corpora that already know their dates.</p>
<p><strong>One selection, four views.</strong> Clicking a row in the Sheet (né Grid), a
day in the Calendar, an entry in the Timeline or Cards routes preview to
the source page with its bounding box, reader to the text, and the
inspector to the entry itself — not its parent. Arrow keys walk the
chronology; ⇧ extends; the status line says "1 of 160 dates selected —
January 14, 1918" instead of counting a list you aren't looking at.</p>
<p><strong>The calendar is the display.</strong> Day cells grow with the window and carry
the day's words; the below-the-grid list is gone. The Sheet ships Date
first, Text second, Name hidden — with native column show/hide and drag
reordering, and multi-line text on demand.</p>
<p><strong>Diary extraction honesty.</strong> Entry spans survive line breaks (far more
entries get boxes), printed date headings are stripped structurally for
old and new data alike, and the prompt now forbids moving one day's
writing under another day's heading.</p>
]]></description>
            <enclosure
                url="https://github.com/dtubb/fichero/releases/download/v2026.08.17/Fichero.dmg"
                length="564305294"
                type="application/octet-stream"
                sparkle:edSignature="mnLlQFU2y/Q7apwXuel3pjpK7l6M/vTNwU1PrDFyuVuWkSaWZN+G1CBPzZgN/9bXuXatOWCV91i4ABOYICIIDg=="
            />
        </item>
        <item>
            <title>Fichero 2026.08.02</title>
            <pubDate>Sun, 02 Aug 2026 16:09:39 -0300</pubDate>
            <sparkle:version>2026080201</sparkle:version>
            <sparkle:shortVersionString>2026.08.02</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<h3>Dev build</h3>
<p>The overnight build: 122 commits on top of the morning's 2026.07.29
release — the workflow-trust overhaul, the iPhone launch fix, and a
night-long bug sweep. Internal TestFlight + DMG dev prerelease from
green <code>integration</code> (1,990 XCTests + 1,927 Swift Testing + 8,500+
Python tests, 0 failures).</p>
<h3>Workflows you can trust</h3>
<p><strong>Every run tells you what it did.</strong> Artifacts now record which run and
step produced them; the artifact browser groups results by run in
pipeline order with "Produced by → View Run" navigation; and a new
Trace tab on every run shows the executed graph — each node colored by
what happened, with the provider and model actually used, timings, and
per-step outputs one click away.</p>
<p><strong>Results land where you're looking.</strong> Transcriptions appear in the
content pane the moment a workflow writes them — no reselecting, from
any window, even runs launched from the CLI.</p>
<p><strong>Honest lifecycle.</strong> Cancel stops multi-file steps within one file;
paused runs can be resumed, cancelled, or deleted; failed runs release
their documents instead of spinning forever; Cancelled shows as
Cancelled; and Pause/Resume/Stop buttons visibly do what they say.</p>
<p><strong>A node editor that explains itself.</strong> Ports and connection types are
finally visible with distinct parallel edges, fan-out badges reflect
real behavior, execution order is numbered on the canvas, a Tidy
command lays the graph out, drops land under the cursor, and the
palette only offers tools that actually run. Zoom nodes show a live
tile-grid preview.</p>
<p><strong>Works out of the box.</strong> A fresh install with no API keys runs every
default workflow on-device; a workflow that needs a missing key says so
before running, naming the provider. Default presets cleaned up
(internal components hidden from Run menus, explicit routing, real
source nodes).</p>
<h3>Fixed</h3>
<p><strong>iPhone launches again.</strong> The iOS TestFlight build crashed instantly at
launch: the shell's fully composed view type overflowed the iPhone's 1MB
main-thread stack during type-metadata instantiation (macOS's 8MB stack
absorbed the identical code). Bounded type-erasures at the compact layout's
chokepoints fix it (#4331).</p>
<p><strong>Edits can no longer vanish.</strong> A rejected page-content save silently marked
the buffer clean, so the next refresh replaced your edit with stale content.
Saves are now transactional: a failure keeps the buffer dirty, shows an
inline error, and retries — and write conflicts with a running workflow
resolve with retries server-side instead of surfacing as an unexplained
error (#4285, #4286).</p>
<p><strong>Table sorting can't crash.</strong> Sorting by Name crashed the app when the
persisted sort field had no matching table column; sort state now only emits
column-backed descriptors (#4282).</p>
<p><strong>Sidebar behaves.</strong> Chevron prefetch restored (#4294), selection survives
tree rebuilds (#4297), drop-hover highlights only the target row instead of
washing the whole subtree (#4229), folders dropped at the library root import
at the root instead of vanishing into Inbox (#4274), and per-row progress
spinners reflect actual running work regardless of selection (#4295).</p>
<p><strong>Workflows tell the truth.</strong> Single-page runs no longer widen to the whole
PDF (#4298); runs where every file failed record as failed with the real
error instead of a green checkmark (#4283); newly added AI providers appear
in the Run Workflow menu immediately, from any window or device (#4276); and
a connection error in the status island clears itself once the server is
reachable again (#4296).</p>
<h3>New</h3>
<p><strong>iPhone essentials.</strong> Tapping a document on iPhone opens the reader again
(#2666), first-run setup skips the Mac-only steps (#2807), and list rows get
native swipe-to-delete (#2501).</p>
<p><strong>Recognized-text boxes.</strong> Every OCR/transcription pass now stores the text's
bounding geometry, and the reader gains a Text Boxes overlay showing exactly
where each recognized line sits on the page (#4309).</p>
<p><strong>Convert to Markdown, HTML, and SVG.</strong> New AI conversion workflows render a
page into portable formats, viewable in place — flip a page between its image
and its generated rendition in the reader (#4329).</p>
<p><strong>Chat with your library.</strong> The research chat now answers through audited,
read-only library tools with each tool call visible and attributed, and
creating a chat works without an AI provider configured (#2067, #4308).</p>
]]></description>
            <enclosure
                url="https://github.com/dtubb/fichero/releases/download/v2026.08.02/Fichero.dmg"
                length="541322988"
                type="application/octet-stream"
                sparkle:edSignature="0EqwmqUkRBSPH/9qkU+SiNNv89/jxStCMw4MEn/2yxcI57cNtUF+fp31MFz6FUHIvl5McHzW1Bcp1CYldARHDQ=="
            />
        </item>
        <item>
            <title>Fichero 2026.07.29</title>
            <pubDate>Wed, 29 Jul 2026 14:06:16 -0300</pubDate>
            <sparkle:version>2026072901</sparkle:version>
            <sparkle:shortVersionString>2026.07.29</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<h3>Dev build</h3>
<p>Internal TestFlight + DMG dev prerelease cut from green <code>integration</code> — the
first release gated end-to-end by the new serialized test harness (full unit,
engine, transport-matrix, and UI-session legs all green).</p>
<p><strong>Fixed</strong>
- Sidebar rows no longer jump after the list loads; root ordering is stable.
- Sidebar clicks are faster: the selection path uses an O(1) index instead of
  re-walking the whole tree, prefetch batches its cache writes, and disclosure
  toggles no longer trigger needless preference writes.
- Folder selection and drops show a loading/importing state immediately
  instead of a dead interval or a false "No Documents".
- Import status can no longer stick at "5/5" after an import finishes.
- Thumbnails: intermittent 500s fixed (alias sync is idempotent and atomic);
  imported images now get thumbnails via a background derivative stage instead
  of staying "Pending"; HEIC photos are supported.
- Legacy .doc files that failed with "Malformed MiniFAT" now extract via a
  fallback reader.
- iCloud placeholder files are refused loudly at import instead of importing
  as empty records.
- Ingest and serving path rules are unified — anything importable is servable.
- Canvas (2D/3D) view modes share the library selection with the list views.
- Engine startup prints a transport diagnostic naming exactly what bound
  where and what a client must set (ends the socket-path guessing game).</p>
<p><strong>App icon</strong>
- New Icon Composer app icon.</p>
]]></description>
            <enclosure
                url="https://github.com/dtubb/fichero/releases/download/v2026.07.29/Fichero.dmg"
                length="534896462"
                type="application/octet-stream"
                sparkle:edSignature="0nHcZqLamDm7ASj8WoxMeDWNP8105TM1HEe1oUiLh5SOPLBQscXzBk9AapVXZEKUkl/Xd28GJ1dnDr3oC1nkBQ=="
            />
        </item>
        <item>
            <title>Fichero 2026.07.26</title>
            <pubDate>Sun, 26 Jul 2026 23:42:28 -0300</pubDate>
            <sparkle:version>2026072601</sparkle:version>
            <sparkle:shortVersionString>2026.07.26</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<h3>Dev build</h3>
<p>Internal TestFlight + DMG dev prerelease cut from green <code>integration</code>.</p>
<h3>New</h3>
<p><strong>Instant launch.</strong> The window mounts real content on the first frame:
saved libraries materialize before the scene graph builds, and nothing
waits for the engine health probe anymore — data streams in as the
engine connects (#4036). Warm first frame dropped from ~1.9s to ~1.4s,
and it shows your library shell instead of a spinner.</p>
<p><strong>Xcode-style status island.</strong> Engine state, background activity, and
errors now live in one center toolbar island beside the title — a
message area ("Starting engine…", import progress, running workflows,
errors in red) flanked by the engine button (connection details +
Retry) and the activity button (task list). The full-window
"Connecting to backend…" takeover and the login-wall flash at launch
are gone; a broken connection is visible chrome, never a blocked app.</p>
<p><strong>Finder-grade sidebar.</strong> The full drag grammar landed: insertion-line
drops, Option-drag copies (through the audited <code>document.duplicate</code>
deep-copy action), Finder-style aliases (badge, target resolution,
Make Alias), Duplicate parity for workflows, saved searches, and
conversations, multi-item drop feedback, count-aware Delete, and
right-arrow handing keyboard focus from a leaf row to the content
pane. Every library gets its own header row, and the pinned bottom
navigation rows are retired. Document-scoped chat is back on Mac as a
context-menu command on the document ("Add to Chat").</p>
<h3>Fixed</h3>
<ul>
<li><strong>100% CPU at idle</strong> — a per-frame UserDefaults write spun the
  AttributeGraph; the app idles at 0% again.</li>
<li><strong>Default Workflows tree</strong> — presets seed into the global library
  only, subfolder ids route correctly (no embedded slashes), legacy
  preset folders that lingered at the sidebar's top level re-home
  under the locked "Default Workflows" container on next open, the
  container's read-only lock is enforced on document actions, and
  re-seeding no longer resurrects soft-deleted workflow mirrors.</li>
<li><strong>One path, not four</strong> — the location breadcrumb renders only in the
  toolbar; the duplicate in-window path bars are gone and the bottom
  status bar is Finder-style (what's selected, not where it is).</li>
<li><strong>Cycle-creating document moves are rejected</strong> by the engine.</li>
<li>Restored-selection reconcile + launch invalidation storm fixed.</li>
</ul>
<h3>Under The Hood</h3>
<p>The macOS verify leg is genuinely green: a MainActor-isolation crash
that killed the test host mid-run, three stale contracts hidden from
failure greps, and a sub-millisecond Date flake are fixed. New engine
tests cover the Default Workflows tree heal.</p>
]]></description>
            <enclosure
                url="https://github.com/dtubb/fichero/releases/download/v2026.07.26/Fichero.dmg"
                length="526768845"
                type="application/octet-stream"
                sparkle:edSignature="P3gTXrGeUCNdFd72rTlQAvK02UXXtgzxzWbBODd3YaX/YQ9/T5AL9tpYsCuTz907opeVVFJjFwW1OZbifRk7CA=="
            />
        </item>
        <item>
            <title>Fichero 2026.07.23</title>
            <pubDate>Thu, 23 Jul 2026 22:05:36 -0300</pubDate>
            <sparkle:version>2026072301</sparkle:version>
            <sparkle:shortVersionString>2026.07.23</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<h3>Dev build</h3>
<p>Internal TestFlight + DMG dev prerelease cut from green <code>integration</code>. Not
promoted to production.</p>
<h3>Under The Hood</h3>
<p><strong>Connection hygiene complete.</strong> Every app↔engine call now routes through the
centralized <code>FicheroClient</code> transport (UDS / HTTPS-when-sharing / in-memory) —
no remaining hand-rolled <code>URLSession</code> bypasses. <code>EntityService</code> (the inspector's
"0 entities" bug), <code>ImageEditingService</code> preview, the knowledge-graph web pane,
and the workflow diagram were all migrated. The KG web pane works over UDS via a
<code>WKURLSchemeHandler</code> bridge (no new network listener), and the workflow diagram
now renders live mermaid (<code>mermaid.js</code> in a <code>WKWebView</code>) instead of a broken
JSON-as-image.</p>
<p><strong>Legible failures.</strong> A typed <code>ConnectionError</code> classifies transport failures by
<code>{transport, operation, cause}</code> so an error names itself instead of a bare
<code>NSURLErrorDomain -1004</code>. A wrapped-cancellation fix (<code>Error.isCancellationError</code>
across ~60 sites) stops superseded inspector/store loads from mislogging as
failures.</p>
<p><strong>Fixes.</strong> Sidebar folder-node click (workflow/search/chat folders showed
nothing) now navigates correctly.</p>
<p><strong>Engine cleanup + speed.</strong> The 61 flat re-export shims left by the package reorg
were removed and 487 callers repointed to the restructured paths; <code>cryptography</code>
was deferred off the startup import graph (−50 modules). ModelComparison moved to
an <code>@Observable</code> store (#1863). Full engine suite green (7955 passed / 0 failed).</p>
]]></description>
            <enclosure
                url="https://github.com/dtubb/fichero/releases/download/v2026.07.23/Fichero.dmg"
                length="508358932"
                type="application/octet-stream"
                sparkle:edSignature="mXzxO3Ll70FTocPJNpO6Kgf4WpvojaqrI8o81zKYAgQ6p8a3SVkBO412b+r80Rn+HeTrLeiN3D/NOHwa4E5cDw=="
            />
        </item>
        <item>
            <title>Fichero 2026.07.20-beta</title>
            <pubDate>Mon, 20 Jul 2026 15:13:56 -0300</pubDate>
            <sparkle:version>2026072001</sparkle:version>
            <sparkle:shortVersionString>2026.07.20-beta</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<h3>New</h3>
<p><strong>Workflow nodes in the sidebar.</strong> Saved workflows now appear in the library
tree. Built-in presets are grouped under a locked <strong>Default Workflows</strong>
hierarchy; duplicate a default to create an editable library copy.</p>
<h3>Improved</h3>
<p><strong>Settings navigation.</strong> The Settings detail pane now has System
Settings-style back and forward controls plus a clearer section header.</p>
<h3>Testing Build</h3>
<p>This prerelease uses Fichero's development feature tier, exposing every
implemented app surface and workflow tool for testing.</p>
<h3>Under The Hood</h3>
<p>Large SwiftUI files across the app shell, library, sidebar, previews,
Inspector, services, and platform bridges were split into focused extensions
without changing behavior. Source-contract tests were updated to follow the
new file boundaries.</p>
]]></description>
            <enclosure
                url="https://github.com/dtubb/fichero/releases/download/v2026.07.20-beta/Fichero.dmg"
                length="511147476"
                type="application/octet-stream"
                sparkle:edSignature="kvkE8goo4h0+Nqt5H3GxHAdqnL/I3g/NPLOv2Z0BvAJezHOBL8obM9ZbvgjknTSH/WLBkDdjhMStJ4iGjcZGAg=="
            />
        </item>
        <item>
            <title>Fichero 2026.07.19.2</title>
            <pubDate>Sun, 19 Jul 2026 21:51:59 -0300</pubDate>
            <sparkle:version>2026071902</sparkle:version>
            <sparkle:shortVersionString>2026.07.19.2</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<h3>Improved</h3>
<p><strong>RealityKit canvas rendering.</strong> The 2D Canvas and 3D Space views now use the
RealityKit renderers by default. The previous renderers remain available as a
revert path while the new interaction and visual behavior is validated.</p>
<p><strong>Clearer Settings.</strong> Settings now uses a System Settings-style sidebar with
clearer section names, colored icons, and pairing controls grouped under
Sharing.</p>
<h3>Fixed</h3>
<ul>
<li>Release builds now reconstruct the embedded Briefcase engine from current
  source every time instead of reusing a potentially stale staged bundle.</li>
<li>The app, embedded engine bundle, and installed Python package now carry the
  same release version.</li>
<li>Newest-first activity ordering now handles ISO 8601 timestamps both with and
  without fractional seconds.</li>
<li>Switching libraries now preserves the app's configured secure network
  session, and Boolean activity metadata displays as <code>true</code>/<code>false</code> instead of
  <code>1</code>/<code>0</code>.</li>
<li>Invalid action payloads now fail safely instead of crashing the app. Legacy
  decomposed library paths normalize correctly, and whitespace-only searches
  remain a local no-op instead of reaching the engine.</li>
</ul>
<h3>Under The Hood</h3>
<p>The Inspector, embedded-engine lifecycle service, chat views, and entity
service were split into smaller concern-focused files without changing their
behavior. The release gate also resolves the shared project Python correctly
when run from a git worktree.</p>
]]></description>
            <enclosure
                url="https://github.com/dtubb/fichero/releases/download/v2026.07.19.2/Fichero.dmg"
                length="495351001"
                type="application/octet-stream"
                sparkle:edSignature="rzxQf9Do+iH8GCJUKBW0DRAcbQvIi3n28fSGTFDXXfiYGoq88Y2zHOPIeizuiNZdReV/qjBjVKMaGkAGboCKBQ=="
            />
        </item>
        <item>
            <title>Fichero 2026.07.19</title>
            <pubDate>Sun, 19 Jul 2026 10:22:00 -0300</pubDate>
            <sparkle:version>2026071901</sparkle:version>
            <sparkle:shortVersionString>2026.07.19</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<h3>Improved</h3>
<p><strong>Faster, more reliable local startup.</strong> Fichero now owns the embedded engine at
the app level instead of tying it to a window. Heavy AI, workflow, image, and
network imports are deferred until they are needed; TLS preparation, token
reads, and package creation no longer block the first frame; and a proven local
readiness result is reused without leaking across a host switch.</p>
<p><strong>Safer imports and library lifecycle.</strong> Path-based imports are confined to
approved roots, Move and Link modes reach the engine intact, route-based ingest
now emits audit and change events, and completed ingest tasks are bounded. When
a library closes in the app it also closes in the engine.</p>
<p><strong>More dependable workflows, exports, and knowledge data.</strong> Every bundled
workflow preset now passes the execution validator, including the paleography
ensemble workflow. Parent-document exports include page-child transcription,
repeated knowledge claims are stored once with a mention count, and incomplete
task results fail instead of being reported as successful.</p>
<h3>Fixed</h3>
<ul>
<li>Embedded-engine shutdown now drains live-update streams instead of force
  killing the process after a timeout.</li>
<li>A broken iPhone or iPad pairing can return to the QR setup screen, and a
  single transient connection blip no longer immediately opens the outage pane.</li>
<li>Paired remote devices can again load their permitted libraries and live
  update streams.</li>
<li>Spawned engines no longer inherit environment flags that can disable
  authentication, while app-wide authorization-library requests no longer
  carry an unrelated library header.</li>
<li>Cancelled thumbnail requests are no longer logged as image failures.</li>
</ul>
<h3>Under The Hood</h3>
<p>The dormant Swift test target was restored and expanded, and the frontend
source tree was reorganized by app surface without changing behavior. The
release gate now covers the real macOS test plan and the current per-platform
Xcode schemes.</p>
]]></description>
            <enclosure
                url="https://github.com/dtubb/fichero/releases/download/v2026.07.19/Fichero.dmg"
                length="495476238"
                type="application/octet-stream"
                sparkle:edSignature="G6j1PESfxA0Cj+DtM7OPyZCN6im9dS2Ug+RyEaNfYqNe/fvo6mXHFZV3R/ElxXyarg1CyWXrzKBD3qN8P67zDg=="
            />
        </item>
        <item>
            <title>Fichero 2026.07.17</title>
            <pubDate>Fri, 17 Jul 2026 04:26:52 -0300</pubDate>
            <sparkle:version>2026071701</sparkle:version>
            <sparkle:shortVersionString>2026.07.17</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<h3>Improved</h3>
<p><strong>Faster embedded-engine startup.</strong> The bundled engine now ships with
precompiled Python bytecode, waits until it is ready before restoring saved
libraries, and binds before doing optional heavyweight work. It no longer opens
every known library or warms embeddings during local startup.</p>
<p><strong>Faster library UI.</strong> This build removes an artifact/entity N+1 fetch,
avoids unnecessary sidebar rebuilds, moves full-image decoding off the main
thread, and improves library filtering, reader, and knowledge-graph work.</p>
<p><strong>Paleography workflows.</strong> New zoom/image-preparation tools, ensemble
transcription, and deterministic consistency checks are available in the
workflow library.</p>
<h3>Fixed</h3>
<ul>
<li>Fixed local and embedded engines incorrectly showing a sign-in wall. A
  loopback engine now treats its host as the owner and keeps library-scope
  failures scoped to that library instead of calling them authentication
  failures.</li>
<li>Fixed healthy launches briefly rendering as <em>Backend Not Connected</em>
  and removed a launch-blocking move-to-Applications modal.</li>
<li>Fixed sandboxed builds opening a library added after engine launch, and
  hardened App Store helper signing and embedded-engine packaging.</li>
<li>Fixed workflow selection so one click opens the selected workflow in the
  node editor.</li>
</ul>
]]></description>
            <enclosure
                url="https://github.com/dtubb/fichero/releases/download/v2026.07.17/Fichero.dmg"
                length="493659856"
                type="application/octet-stream"
                sparkle:edSignature="phRf8mabLIaOA2u4/SuMavX1v2pLPC6hrzxWP70880tFM3G1NKZF6dTYlv6WIAZe9AsEcgHHBD/Wd/bWLfNOAQ=="
            />
        </item>
        <item>
            <title>Fichero 2026.07.10-beta</title>
            <pubDate>Fri, 10 Jul 2026 14:29:00 -0300</pubDate>
            <sparkle:version>2026071001</sparkle:version>
            <sparkle:shortVersionString>2026.07.10-beta</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<p>The first notarized build, auto-updating via Sparkle.</p>
<h3>New</h3>
<p><strong>Local models, managed for you.</strong> Fichero can now download, store, and run
local models itself — a supervised MLX sidecar with its own isolated runtime,
gated on hardware that can actually run it. No terminal, no separate server.
Apple Intelligence and Apple Vision remain fully on-device options.</p>
<p><strong>Knowledge Graph, grown up.</strong> Claims and entities now carry attribution —
speaker, quotation kind, language, audience, genre, and the source of the
confidence score. Claims link to other claims. Everything scopes to a page, a
document, or a folder, and keeps the passage it came from. Entities
de-duplicate; conflicting types get flagged rather than silently merged.</p>
<p><strong>Document Inspector V2.</strong> Tabbed Info / Metadata / Content / Artifacts /
Knowledge Graph, alongside a multi-pane reading layout with a PDF page view and
per-page artifacts. Content is editable in place.</p>
<p><strong>Canvas and Space.</strong> Library contents arrange on a 2D canvas or in a 3D space,
with layouts that persist per library.</p>
<p><strong>Translation.</strong> Translate a document into a language you choose. The
translation is stored as its own artifact, embedded so it turns up in search,
and listed by language in the reader alongside the source. The immersive reader
gains a Source / Diplomatic selector, and every machine-made representation
carries its provenance and an <strong>AI unreviewed</strong> badge until a person says
otherwise.</p>
<p><strong>Bibliography.</strong> A reference panel that extracts citations from a document,
resolves their metadata from a DOI or ISBN, lets you edit it in a native form,
imports references in bulk, and exports BibTeX. Deletes are undoable.</p>
<p><strong>Search.</strong> Results show the matched excerpt in context, not just a filename.
Typos are tolerated, and exact matches rank above semantic neighbours.</p>
<p><strong>Users and sharing.</strong> Fichero now has real user accounts. Libraries can be
shared, access granted and revoked per folder, and every mutation is recorded
with the account that made it. Off by default — a single-user library behaves
exactly as before.</p>
<p><strong>Device pairing.</strong> Pair your own Macs and iPads over the local network with a
QR code and per-device tokens.</p>
<p><strong>Static export.</strong> Export a library as a browsable, offline-searchable static
site with per-entity knowledge pages.</p>
<p><strong><code>fichero</code> command line.</strong> A typed command surface mirroring the engine's HTTP
API — engine lifecycle, library management, import, and a persisted registry of
known libraries.</p>
<p><strong>Primary Language setting</strong>, and NFC path normalization so accented filenames
round-trip correctly between Finder, the database, and disk.</p>
<h3>Improved</h3>
<p><strong>Chat</strong> has a cleaner header, conversation-scoped attachments, and a compact
layout for iPhone and iPad.</p>
<p><strong>Cancellation.</strong> Workflows can be cancelled mid-run, and workflow execution
moved off the main event loop — a slow node no longer freezes the engine.</p>
<p><strong>Multilingual catalogue reliability.</strong> When Apple Intelligence refuses a
locale or trips a safety filter, the run falls back to your configured cloud
model instead of returning an empty catalogue.</p>
<p><strong>Undo</strong> reaches the surfaces that promised it: documents, images, knowledge
graph and artifacts, claim links, annotations, classifications, snapshots,
bookmarks. Every audited action is recorded centrally, so ⌘Z works across the
app rather than in a handful of places — and when an undo fails it says so
instead of quietly doing nothing.</p>
<p><strong>Reading layouts.</strong> Multi-page PDFs can be read one page at a time or several
up, with a layout picker in the reader.</p>
<p><strong>Knowledge graph housekeeping.</strong> A possible-duplicates surface merges entities
in one click, with a picker for which record survives. Repeated claims from
different sources fold into a single canonical row.</p>
<p><strong>Errors say what happened.</strong> Service, research, and per-library history
failures now surface the real message instead of a generic Cocoa error, and the
engine re-probes with backoff to recover a healthy connection rather than
failing the launch outright.</p>
<h3>Security</h3>
<p><strong>Per-launch API token.</strong> The engine binds loopback-only (<code>127.0.0.1</code>) and
requires a startup-generated bearer token
(<code>~/Library/Application Support/Fichero/.api-key</code>, mode <code>0600</code>). Fichero is not
reachable from the internet or your local network; the token closes the
remaining gap of other apps running as you on the same Mac.</p>
<p><strong>Audited writes.</strong> Every backend mutation routes through one audited action
layer that records what changed and which account changed it.</p>
<p><strong>Path confinement.</strong> A lexical <code>..</code> traversal in the library path allowlist is
closed, and the QuickLook preview sanitizes a server-supplied filename before
using it as a path. Annotation geometry and colour are validated on the way in.</p>
<p><strong>Fail loud, not quiet.</strong> Export provenance gaps, importer degradation, and
startup misconfiguration now surface as errors instead of silently substituting
a default. A workflow fan-out that fails completely reports the failure rather
than returning an empty result, and values the pipeline cannot interpret are
routed to human review instead of guessed at.</p>
<h3>Fixed</h3>
<ul>
<li><strong>Launch crash.</strong> Opening a library window could crash the app: SwiftUI was
  registering the search field twice, once globally and again in individual
  mode views. Per-view search now defers to the single toolbar search, and the
  first-run provider sheet waits until the toolbar has laid itself out.</li>
<li><strong>The app could not open its own library.</strong> A sandboxed build was denied
  access to its container path, and a stale API token produced an
  authorization failure on a freshly started engine.</li>
<li><strong>Activity progress and log</strong> stream correctly. The workflow event stream was
  a single-consumer queue that starved a second subscriber, leaving 0% progress
  and an empty log; it is now a fan-out broadcaster with a replay buffer.</li>
<li><strong>Chat</strong> no longer blocks while the model is thinking, and it remembers the
  conversation — earlier turns are included in the prompt, and context survives
  a retry.</li>
<li><strong>Knowledge Graph and the document reader</strong> render again over the pinned
  engine connection.</li>
<li><strong>Per-page transcription</strong> applies across every Transcribe and Catalogue
  preset.</li>
<li><strong>Shell</strong>: iPhone inspector opens full-height; the macOS sidebar selection
  updates the view; the iOS reader hides desktop zoom on compact widths.</li>
<li>Backend 500s on list endpoints, knowledge-graph cascade deletes, LanceDB
  fork-safety, a DuckDB upsert crash, re-OCR of already-digital PDFs, keyword
  over-extraction, and an assortment of inspector, thumbnail, and activity bugs.</li>
</ul>
<h3>Under the hood</h3>
<ul>
<li>Every list endpoint speaks one OpenAPI envelope contract, guarded by a
  permanent endpoint-walker test.</li>
<li>The Swift app talks to the engine through generated, typed operations rather
  than hand-written requests.</li>
<li><code>scripts/verify_all.sh</code> (SwiftLint + Xcode test suite + backend contract
  tests) is the single answer to "is it green", wired to ⌘U, and renders its
  failures to an HTML dashboard.</li>
<li>In Debug the engine runs externally; a Release build embeds and launches it,
  signed with hardened-runtime entitlements.</li>
<li>A launch-crash smoke test boots the built <code>.app</code> and asserts it survives.</li>
<li>Graph retrieval no longer scans the whole table; citation and reference
  filters run in the database.</li>
</ul>
<h3>Known issues</h3>
<ul>
<li>The live-updates event stream (<code>/api/changes/stream</code>) fails TLS on a
  self-signed <code>.local</code> certificate.</li>
<li>IIIF endpoints are staged behind <code>FICHERO_FEATURE_TIER=dev</code> and are off in a
  release build.</li>
</ul>
]]></description>
            <enclosure
                url="https://github.com/dtubb/fichero/releases/download/v2026.07.10-beta/Fichero.dmg"
                length="427854351"
                type="application/octet-stream"
                sparkle:edSignature="HkcOIrQHLNCt/5iBDPCj0v/SfsccRIpz4RmhONHC9omFiFbA7lBwUJ9NZPt7/rHKImN5D6ZDyCmFSM3Nujp/Ag=="
            />
        </item>
        <item>
            <title>Fichero 2026.07.08-beta</title>
            <pubDate>Wed, 08 Jul 2026 12:46:16 -0300</pubDate>
            <sparkle:version>2026070801</sparkle:version>
            <sparkle:shortVersionString>2026.07.08-beta</sparkle:shortVersionString>
            <sparkle:minimumSystemVersion>15.0</sparkle:minimumSystemVersion>
            <description><![CDATA[
<p>The first notarized, auto-updating build.</p>
<h3>New</h3>
<p><strong>Local models, managed for you.</strong> Fichero can now download, store, and run
local models itself — a supervised MLX sidecar with its own isolated runtime,
gated on hardware that can actually run it. No terminal, no separate server.
Apple Intelligence and Apple Vision remain fully on-device options.</p>
<p><strong>Knowledge Graph, grown up.</strong> Claims and entities now carry attribution —
speaker, quotation kind, language, audience, genre, and the source of the
confidence score. Claims link to other claims. Everything scopes to a page, a
document, or a folder, and keeps the passage it came from. Entities
de-duplicate; conflicting types get flagged rather than silently merged.</p>
<p><strong>Document Inspector V2.</strong> Tabbed Info / Metadata / Content / Artifacts /
Knowledge Graph, alongside a multi-pane reading layout with a PDF page view and
per-page artifacts. Content is editable in place.</p>
<p><strong>Canvas and Space.</strong> Library contents arrange on a 2D canvas or in a 3D space,
with layouts that persist per library.</p>
<p><strong>Translation.</strong> Translate a document into a language you choose. The
translation is stored as its own artifact, embedded so it turns up in search,
and listed by language in the reader alongside the source. The immersive reader
gains a Source / Diplomatic selector, and every machine-made representation
carries its provenance and an <strong>AI unreviewed</strong> badge until a person says
otherwise.</p>
<p><strong>Bibliography.</strong> A reference panel that extracts citations from a document,
resolves their metadata from a DOI or ISBN, lets you edit it in a native form,
imports references in bulk, and exports BibTeX. Deletes are undoable.</p>
<p><strong>Search.</strong> Results show the matched excerpt in context, not just a filename.
Typos are tolerated, and exact matches rank above semantic neighbours.</p>
<p><strong>Users and sharing.</strong> Fichero now has real user accounts. Libraries can be
shared, access granted and revoked per folder, and every mutation is recorded
with the account that made it. Off by default — a single-user library behaves
exactly as before.</p>
<p><strong>Device pairing.</strong> Pair your own Macs and iPads over the local network with a
QR code and per-device tokens.</p>
<p><strong>Static export.</strong> Export a library as a browsable, offline-searchable static
site with per-entity knowledge pages.</p>
<p><strong><code>fichero</code> command line.</strong> A typed command surface mirroring the engine's HTTP
API — engine lifecycle, library management, import, and a persisted registry of
known libraries.</p>
<p><strong>Primary Language setting</strong>, and NFC path normalization so accented filenames
round-trip correctly between Finder, the database, and disk.</p>
<h3>Improved</h3>
<p><strong>Chat</strong> has a cleaner header, conversation-scoped attachments, and a compact
layout for iPhone and iPad.</p>
<p><strong>Cancellation.</strong> Workflows can be cancelled mid-run, and workflow execution
moved off the main event loop — a slow node no longer freezes the engine.</p>
<p><strong>Multilingual catalogue reliability.</strong> When Apple Intelligence refuses a
locale or trips a safety filter, the run falls back to your configured cloud
model instead of returning an empty catalogue.</p>
<p><strong>Undo</strong> reaches the surfaces that promised it: documents, images, knowledge
graph and artifacts, claim links, annotations, classifications, snapshots,
bookmarks. Every audited action is recorded centrally, so ⌘Z works across the
app rather than in a handful of places — and when an undo fails it says so
instead of quietly doing nothing.</p>
<p><strong>Reading layouts.</strong> Multi-page PDFs can be read one page at a time or several
up, with a layout picker in the reader.</p>
<p><strong>Knowledge graph housekeeping.</strong> A possible-duplicates surface merges entities
in one click, with a picker for which record survives. Repeated claims from
different sources fold into a single canonical row.</p>
<p><strong>Errors say what happened.</strong> Service, research, and per-library history
failures now surface the real message instead of a generic Cocoa error, and the
engine re-probes with backoff to recover a healthy connection rather than
failing the launch outright.</p>
<h3>Security</h3>
<p><strong>Per-launch API token.</strong> The engine binds loopback-only (<code>127.0.0.1</code>) and
requires a startup-generated bearer token
(<code>~/Library/Application Support/Fichero/.api-key</code>, mode <code>0600</code>). Fichero is not
reachable from the internet or your local network; the token closes the
remaining gap of other apps running as you on the same Mac.</p>
<p><strong>Audited writes.</strong> Every backend mutation routes through one audited action
layer that records what changed and which account changed it.</p>
<p><strong>Path confinement.</strong> A lexical <code>..</code> traversal in the library path allowlist is
closed, and the QuickLook preview sanitizes a server-supplied filename before
using it as a path. Annotation geometry and colour are validated on the way in.</p>
<p><strong>Fail loud, not quiet.</strong> Export provenance gaps, importer degradation, and
startup misconfiguration now surface as errors instead of silently substituting
a default. A workflow fan-out that fails completely reports the failure rather
than returning an empty result, and values the pipeline cannot interpret are
routed to human review instead of guessed at.</p>
<h3>Fixed</h3>
<ul>
<li><strong>Launch crash.</strong> Opening a library window could crash the app: SwiftUI was
  registering the search field twice, once globally and again in individual
  mode views. Per-view search now defers to the single toolbar search, and the
  first-run provider sheet waits until the toolbar has laid itself out.</li>
<li><strong>The app could not open its own library.</strong> A sandboxed build was denied
  access to its container path, and a stale API token produced an
  authorization failure on a freshly started engine.</li>
<li><strong>Activity progress and log</strong> stream correctly. The workflow event stream was
  a single-consumer queue that starved a second subscriber, leaving 0% progress
  and an empty log; it is now a fan-out broadcaster with a replay buffer.</li>
<li><strong>Chat</strong> no longer blocks while the model is thinking, and it remembers the
  conversation — earlier turns are included in the prompt, and context survives
  a retry.</li>
<li><strong>Knowledge Graph and the document reader</strong> render again over the pinned
  engine connection.</li>
<li><strong>Per-page transcription</strong> applies across every Transcribe and Catalogue
  preset.</li>
<li><strong>Shell</strong>: iPhone inspector opens full-height; the macOS sidebar selection
  updates the view; the iOS reader hides desktop zoom on compact widths.</li>
<li>Backend 500s on list endpoints, knowledge-graph cascade deletes, LanceDB
  fork-safety, a DuckDB upsert crash, re-OCR of already-digital PDFs, keyword
  over-extraction, and an assortment of inspector, thumbnail, and activity bugs.</li>
</ul>
<h3>Under the hood</h3>
<ul>
<li>Every list endpoint speaks one OpenAPI envelope contract, guarded by a
  permanent endpoint-walker test.</li>
<li>The Swift app talks to the engine through generated, typed operations rather
  than hand-written requests.</li>
<li><code>scripts/verify_all.sh</code> (SwiftLint + Xcode test suite + backend contract
  tests) is the single answer to "is it green", wired to ⌘U, and renders its
  failures to an HTML dashboard.</li>
<li>In Debug the engine runs externally; a Release build embeds and launches it,
  signed with hardened-runtime entitlements.</li>
<li>A launch-crash smoke test boots the built <code>.app</code> and asserts it survives.</li>
<li>Graph retrieval no longer scans the whole table; citation and reference
  filters run in the database.</li>
</ul>
<h3>Known issues</h3>
<ul>
<li>The live-updates event stream (<code>/api/changes/stream</code>) fails TLS on a
  self-signed <code>.local</code> certificate.</li>
<li>IIIF endpoints are staged behind <code>FICHERO_FEATURE_TIER=dev</code> and are off in a
  release build.</li>
</ul>
]]></description>
            <enclosure
                url="https://github.com/dtubb/fichero/releases/download/v2026.07.08-beta/Fichero.dmg"
                length="431820011"
                type="application/octet-stream"
                sparkle:edSignature="bdB6sougFpn6W3K/Drr1V32fd1vvfP/6Hl61E+GP761St4cuqk4cWiCxTkJJg8uLKzl85Zyl1eFCZhCCPdP5CQ=="
            />
        </item>
    </channel>
</rss>
